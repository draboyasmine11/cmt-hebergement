spring:
  application:
    name: cmt-sonabel-api
  datasource:
    url: jdbc:mysql://localhost:3306/cmt_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: cmt_user
    password: cmt2026
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  jackson:
    serialization:
      write-dates-as-timestamps: false
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:assistance@cmt.bf}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080
  servlet:
    context-path: /api
  tomcat:
    max-swallow-size: -1

cmt:
  jwt:
    secret: ${JWT_SECRET:SonabelCmtSecretKey2026PourJwtTokenSecuriseMinimum256Bits!!}
    expiration-ms: 28800000
  upload:
    dir: uploads
  notification:
    email:
      from: ${MAIL_USERNAME:assistance@cmt.bf}
      support: support@cmt.bf
    sms:
      enabled: false
      api-url: https://api.smsprovider.bf/send
      api-key: ${SMS_API_KEY:}
      sender: CMT-SONABEL
    app:
      enabled: true

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method

logging:
  level:
    com.sonabel.cmt: INFO
    org.springframework.security: WARN
