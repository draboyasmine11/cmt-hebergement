spring:
  application:
    name: cmt-sonabel-api
  datasource:
    url: jdbc:mysql://localhost:3306/cmt_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: cmt_user
    password: cmt2026
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  jackson:
    serialization:
      write-dates-as-timestamps: false
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:m65662530@gmail.com}
    password: ${MAIL_PASSWORD:ugiz hcuv czod vhgs}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080
  servlet:
    context-path: /api
  tomcat:
    max-swallow-size: -1

cmt:
  app:
    url: ${APP_URL:http://localhost:4200}
  jwt:
    secret: ${JWT_SECRET:SonabelCmtSecretKey2026PourJwtTokenSecuriseMinimum256Bits!!}
    expiration-ms: 28800000
  upload:
    dir: uploads
  notification:
    email:
      from: ${MAIL_USERNAME:m65662530@gmail.com}
      support: support@cmt.bf
    sms:
      enabled: true
      provider: ${SMS_PROVIDER:generic}
      api-url: ${SMS_API_URL:https://api.smsprovider.bf/send}
      api-key: ${SMS_API_KEY:}
      sender: CMT-SONABEL
      twilio:
        account-sid: ${TWILIO_ACCOUNT_SID:}
        auth-token: ${TWILIO_AUTH_TOKEN:}
        from-number: ${TWILIO_FROM_NUMBER:}
    app:
      enabled: true

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method

logging:
  level:
    com.sonabel.cmt: INFO
    org.springframework.security: WARN
